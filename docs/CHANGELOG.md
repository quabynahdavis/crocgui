# Changelog — Documentation Root

Revision history for the top-level `/docs` documents (`OVERVIEW.md` and this file). Domain-specific
changes are recorded in the `CHANGELOG.md` of the relevant subdirectory.

## 2026-08-08

### Added
- Initial `/docs` documentation tree covering getting started, architecture, testing, and features.
- `OVERVIEW.md` with a project summary, technology table, and a complete index of every domain and
  topic document.
- Documentation conventions section describing the mandatory `OVERVIEW.md` / `CHANGELOG.md` pair in
  each directory and the sequential numbering scheme for topic documents.
- Reference to the root-level `README.md`, `CONTRIBUTING.md`, and `AGENTS.md`, which remain outside
  `/docs` by design.

## 2026-08-09

### Added
- Documentation for Linux native window decorations fix (removing custom GTK titlebar to show window icon in titlebar).
- Added `gtk` and `gdk-pixbuf` dependencies for Linux window icon setup.
